// app/adapters/todo.js

import LocalStorageAdapter from 'ember-local-storage/adapters/local';
import JSONAPIAdapter from '@ember-data/adapter/json-api';

export default class EmployeeAdapter extends LocalStorageAdapter {
//   namespace = 'employee'; // Make sure this matches the model name
//   headers = {
//     'Content-Type': 'application/json',
//   };
}
